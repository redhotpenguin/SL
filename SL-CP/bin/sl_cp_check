#!/usr/bin/perl

use strict;
use warnings;

use SL::CP::IPTables ();

my $mac = shift || die "$0 FF:FF:FF:FF:FF:FF\n";

my $check = SL::CP::IPTables->_mac_check( $mac );

warn("check returned $check");

